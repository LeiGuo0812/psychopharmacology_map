# Release notes

## v1.7 — 浏览器兼容性更新

软件版本：`v1.7.0`

分发 ID：`app-2026-08-24-browser-compat-v1`

知识数据版本：`kb-2026-08-12-single-ingredient-drugs-v1`（本次未改动医学知识快照）

知识库生成时间：`2026-08-23T16:22:01+00:00`

数据快照日期：`2026-08-12`

在线使用：https://leiguo0812.github.io/psychopharmacology_map/

离线文件：`psychopharmacology-map-offline.html`

SHA-256：`51999e0bef10d73200f452907f75aa753dbccfd65127849733a1e95ecd94cc61`

### 浏览器兼容性改进

- 离线单文件改为面向 Chrome 64+ / Microsoft Edge 79+ 的 classic IIFE 兼容构建，同时保留 `file://` 直接打开能力。
- 桑基图加入传统 `vh` 与动态视口单位的渐进高度回退，旧浏览器不再因 `dvh/svh` 无效而得到零高度画布。
- `ResizeObserver` 缺失时改用首次测量、`requestAnimationFrame` 与窗口 resize 事件，不再因精确测量失败而永久隐藏图谱。
- 移除启动和互动流程中的 `globalThis`、`Object.fromEntries`、`Array.flat/flatMap` 依赖，并增加旧浏览器文本换行、颜色、定位和键盘焦点回退。
- 图形和路径卡继续读取同一套离线数据；兼容性变化不改变药物、靶点、功能、临床现象或来源关系。
- 构建门禁新增兼容目标、CSS 回退、API 缺失模拟及真实旧 Chromium 测试记录（HeadlessChrome/64.0.3282.0）。

### 延续的知识库内容基线

- 药物目录统一为 160 个单一活性实体；删除 `A/B`、`A+B` 与英文 `A and B` 式复方药物节点，机制、分类和互动路径均直接归属各单一成分。
- 修正复方产品标签向单一成分的错误归因：多活性成分标签不再生成单药适应证或单药安全性断言，产品特定适应证也不再进入单药详情。
- 修正宽泛“抑郁”术语误映射为产后抑郁的问题，并增加复方产品边界的自动验证门禁。
- 将美金刚的 NMDA 作用改为更准确的“不竞争性开放通道阻断”，并区分曲司氯铵单药不良反应证据与 M2/M3 亚型机制推断。
- 单药拆分审计现逐条记录旧复方路径到单药路径、机制替代和证据边界，禁止仅凭节点计数宣称迁移完整。
- 保留地达西尼及此前补入的常用药、新药和生物制剂；生物制剂仍按单一活性实体收录。
- 复方产品资料仅作为证据边界或推断背景保留，不作为药物节点，也不用于暗示可拆分的单药临床贡献。
- 新增明确标记的“学习推断”临床路线；推断依据与局限只作为学习线索，不改写为已证实临床因果。
- 保留可变深度互动路径；证据等级或映射不明确时以注释说明，不因证据不足而静默删除知识。
- 支持药物属于多个一级类别，并保留规范主分类与其他临床相关分类。
- 解除互动界面的同类比较限制，可将任意一级类别中的两种或多种药物加入同一比较清单。
- 具体机制功能可审核时显示具体变化；尚不能具体化时使用明确标记的笼统功能说明。
- 靶点分布加入中文文本、稳定身份匹配审计，以及基于显式靶点层级的成员记录汇总。
- 药物亲和力/效价信息在悬停中显示具体数值，在详情中保留完整记录。
- 调整首页入口配色、分类标签布局、跨类别比较清单和详情页折叠层级。

### 公开载荷计数

- 160 个单一活性药物实体
- 1138 条互动路径
- 151 个公开来源目录项
- 423 条公开靶点分布记录
- 68 条靶点分布成员语义连接
- 4576 条定量亲和力/效价记录
- 134 份药品标签文档

### 分发与审核边界

离线 HTML 已通过单文件完整性、无网络依赖、版本一致性、公开载荷计数和敏感内容门禁。内部参考书来源、内部来源 ID、本机路径和页码定位均未进入 GitHub 分发文件。

本版本仍是医学审核草稿，不是正式医学发布：`medical_signoff=false`，`public_release_allowed=false`。
